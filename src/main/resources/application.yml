
server:
  port: 9090
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: Shoxruh0912
    username: postgres
    url: jdbc:postgresql://localhost:5432/PDPMealSystem
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect

